name: Build Android APK (Kivy)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      ANDROID_SDK_ROOT: /home/runner/.buildozer/android/platform/android-sdk
      ANDROID_HOME: /home/runner/.buildozer/android/platform/android-sdk
      ANDROIDSDK: /home/runner/.buildozer/android/platform/android-sdk
      P4A_NO_SDK_UPDATE: "1"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Install Linux deps
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            git zip unzip curl \
            build-essential ccache \
            libffi-dev libssl-dev \
            autoconf automake libtool pkg-config

          python -m pip install --upgrade pip
          pip install buildozer cython

      - name: Install Android cmdline-tools + pin SDK to 34.0.0 (HARD)
        shell: bash
        run: |
          set -euo pipefail
          SDKROOT="$ANDROID_SDK_ROOT"
          mkdir -p "$SDKROOT"

          # 1) cmdline-tools 설치
          cd /tmp
          curl -L -o sdk.zip https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
          rm -rf /tmp/cmdline && mkdir -p /tmp/cmdline
          unzip -q sdk.zip -d /tmp/cmdline

          rm -rf "$SDKROOT/cmdline-tools/latest"
          mkdir -p "$SDKROOT/cmdline-tools/latest"
          cp -r /tmp/cmdline/cmdline-tools/* "$SDKROOT/cmdline-tools/latest/"

          mkdir -p "$SDKROOT/tools/bin"
          ln -sf "$SDKROOT/cmdline-tools/latest/bin/avdmanager" "$SDKROOT/tools/bin/avdmanager" || true

          # 2) 라이선스 파일(추가 안전장치)
          mkdir -p "$SDKROOT/licenses"
          {
            echo "8933bad161af4178b1185d1a37fbf41ea5269c55"
            echo "d56f5187479451eabf01fb78af6dfcb131a6481e"
          } > "$SDKROOT/licenses/android-sdk-license"
          echo "84831b9409646a918e30573bab4c9c91346d8abd" > "$SDKROOT/licenses/android-sdk-preview-license"

          # 3) ✅ sdkmanager를 "가로채는" wrapper 생성
          REAL_SDKMANAGER="$SDKROOT/cmdline-tools/latest/bin/sdkmanager"
          cat > "$SDKROOT/tools/bin/sdkmanager" <<'WRAP'
          #!/usr/bin/env bash
          set -euo pipefail

          # 실제 sdkmanager 위치는 환경변수로 전달
          REAL="${REAL_SDKMANAGER_PATH:-}"

          if [[ -z "${REAL}" || ! -x "${REAL}" ]]; then
            echo "REAL sdkmanager not found: REAL_SDKMANAGER_PATH=${REAL}" >&2
            exit 1
          fi

          # 인자에서 rc/preview build-tools 요청을 제거(차단)
          filtered=()
          for a in "$@"; do
            if [[ "$a" =~ build-tools\;37 ]] || [[ "$a" =~ 37\.0\.0-rc ]] || [[ "$a" =~ 37-rc ]]; then
              echo "BLOCKED (rc build-tools): $a" >&2
              continue
            fi
            filtered+=("$a")
          done

          exec "$REAL" "${filtered[@]}"
WRAP
          chmod +x "$SDKROOT/tools/bin/sdkmanager"

          # wrapper가 real 위치를 알 수 있게 환경변수로 export(현재 step + 이후 step)
          echo "REAL_SDKMANAGER_PATH=$REAL_SDKMANAGER" >> $GITHUB_ENV

          # PATH 등록
          echo "$SDKROOT/cmdline-tools/latest/bin" >> $GITHUB_PATH
          echo "$SDKROOT/tools/bin" >> $GITHUB_PATH
          echo "$SDKROOT/platform-tools" >> $GITHUB_PATH
          echo "$SDKROOT/build-tools/34.0.0" >> $GITHUB_PATH

          # 4) 라이선스 동의(비대화)
          yes | "$SDKROOT/tools/bin/sdkmanager" --sdk_root="$SDKROOT" --licenses

          # 5) ✅ stable 채널 + 34.0.0만 설치
          "$SDKROOT/tools/bin/sdkmanager" --sdk_root="$SDKROOT" --channel=0 \
            "platform-tools" \
            "platforms;android-34" \
            "build-tools;34.0.0"

          # 6) aidl 확인
          ls -al "$SDKROOT/build-tools" || true
          ls -al "$SDKROOT/build-tools/34.0.0" || true
          test -f "$SDKROOT/build-tools/34.0.0/aidl"
          echo "✅ aidl OK"

      - name: Diagnostics
        shell: bash
        run: |
          set -e
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT"
          echo "REAL_SDKMANAGER_PATH=$REAL_SDKMANAGER_PATH"
          which sdkmanager
          sdkmanager --version || true
          ls -al "$ANDROID_SDK_ROOT/build-tools" || true
          ls -al "$ANDROID_SDK_ROOT/build-tools/34.0.0" || true
          ls -al "$ANDROID_SDK_ROOT/licenses" || true

      - name: Build APK
        shell: bash
        run: |
          set -euo pipefail

          # ✅ 빌드 단계에서 다시 강제(중요)
          export ANDROID_HOME="$ANDROID_SDK_ROOT"
          export ANDROID_SDK_ROOT="$ANDROID_SDK_ROOT"
          export ANDROIDSDK="$ANDROID_SDK_ROOT"
          export REAL_SDKMANAGER_PATH="$REAL_SDKMANAGER_PATH"

          export PATH="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/tools/bin:$ANDROID_SDK_ROOT/platform-tools:$ANDROID_SDK_ROOT/build-tools/34.0.0:$PATH"

          # 추가 방어
          export P4A_NO_SDK_UPDATE=1

          buildozer -v android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: bin/*.apk
